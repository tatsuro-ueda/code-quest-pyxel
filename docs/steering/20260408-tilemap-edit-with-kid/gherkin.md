# 受け入れ条件: 子どもといっしょにタイルマップを編集する

## プロダクト判断の合意事項

| # | 論点 | 決定 | 理由 |
|---|---|---|---|
| T1 | Code Maker への動線 | ゲーム内メニューに**「Code Makerをひらく」**を追加 | 親が「ブックマークどこだっけ」と探す時間を消す |
| T2 | 編集結果の反映方法 | **ブラウザリロードで完結**する | 子どもが待てる限界は30秒 |
| T3 | 歩行可否のデフォルト | タイルごとに**デフォルトの歩行可否が決まっている** | 「歩けると思ったら歩けない」混乱を防ぐ |
| T4 | オートタイルの振る舞い | 編集後も**自動で繋ぎ直す** | 道が切れる・水辺が崩れると親が「やめておこう」となる |
| T5 | 元に戻す手段 | **Ctrl+Z** または **タブを閉じれば元の状態** | 子どもが「ちがった」と言える環境が試行回数を増やす |
| T6 | zip 転送手順 | **手動ドラッグ&ドロップ**で完結する（ダウンロード→VMにD&D） | 段取りに気を取られると子どもが置き去りになる。自動同期は将来検討 |

### 判断の依存関係

```mermaid
graph TD
    T1[T1: Code Maker動線<br/>メニューから1クリック] --> T2[T2: リロードで反映]
    T2 --> T6[T6: zip転送<br/>ワンステップ]

    T3[T3: 歩行可否<br/>デフォルト決定] --> T4[T4: オートタイル<br/>自動繋ぎ直し]

    T5[T5: 元に戻す<br/>Ctrl+Z / タブ閉じ]

    style T1 fill:#4a9,color:#fff
    style T3 fill:#4a9,color:#fff
```

---

## シナリオ全体マップ

```mermaid
graph TD
    START[親子がCode Makerを開く] --> S1[シナリオ1<br/>タイル配置→リロード→反映]

    S1 --> S2[シナリオ2<br/>オートタイルが自動で繋がる]
    S1 --> S3[シナリオ3<br/>歩行可否がデフォルトで決まる]
    S1 --> S4[シナリオ4<br/>元に戻せる]

    S1 --> S5{失敗系}
    S5 --> S6[シナリオ5<br/>編集内容が黙って消えない]
    S5 --> S7[シナリオ6<br/>ゲームが起動しなくならない]

    S8[シナリオ7<br/>子どもがマウスで操作できる]

    style S1 fill:#49a,color:#fff
    style S6 fill:#a44,color:#fff
    style S7 fill:#a44,color:#fff
    style S8 fill:#4a9,color:#fff
```

---

## シナリオ

### シナリオ1: タイルを配置してリロードで反映される（K1, K2）

```gherkin
Given 親が Code Maker のタイルマップエディタを開いている
And 子どもが横で画面を見ている
When 子どもが木のタイルをクリックしてマップに配置する
And 保存してzipをダウンロードする
And zipをVMに転送してブラウザをリロードする
Then ゲーム内で配置した位置に木が表示される
And 編集前と異なるタイルが反映されている
```

```mermaid
graph TD
    A[タイルマップエディタを開く] --> B[子どもが木タイルを<br/>クリックして配置]
    B --> C[保存→zipダウンロード]
    C --> D[VMに転送]
    D --> E[ブラウザリロード]
    E --> F[配置した位置に<br/>木が表示される]

    style B fill:#4a9,color:#fff
    style F fill:#4a9,color:#fff
```

### シナリオ2: オートタイルが編集後も自然に見える（K4）

```gherkin
Given マップ上に道が繋がっている状態がある
When 道の隣のタイルを別の地形に変更する
Then 道のオートタイルが自動で繋ぎ直される
And 道が不自然に途切れない
```

```mermaid
graph TD
    A[道が繋がっている] --> B[隣のタイルを変更]
    B --> C{オートタイル}
    C --> D[道が自動で繋ぎ直される]
    C --> E[水辺の縁取りも<br/>自動で調整される]

    style D fill:#4a9,color:#fff
    style E fill:#4a9,color:#fff
```

### シナリオ3: 歩行可否がデフォルトで決まる（K5）

```gherkin
Given 子どもがタイルマップエディタで編集している
When 木のタイルを配置する
Then そのタイルは「歩けない」がデフォルトで設定される
And ゲーム内でキャラクターがそのタイルに進入できない
```

```mermaid
graph TD
    A[タイルを配置] --> B{タイル種別}
    B -->|草・道・砂| C[歩ける<br/>デフォルト]
    B -->|木・山・水・壁| D[歩けない<br/>デフォルト]
    C --> E[ゲームで通過できる]
    D --> F[ゲームで通過できない]

    style C fill:#4a9,color:#fff
    style D fill:#a94,color:#fff
```

### シナリオ4: 編集を元に戻せる（T5, KR3）

```gherkin
Given 子どもがタイルを配置した直後
When Ctrl+Z を押す
Then 直前の配置が取り消される
And マップが配置前の状態に戻る
```

```mermaid
graph TD
    A[タイルを配置] --> B[あ、ちがった！]
    B --> C{元に戻す方法}
    C -->|Ctrl+Z| D[直前の操作を取り消し]
    C -->|タブを閉じて<br/>再アップロード| E[保存前の状態に戻る]

    style D fill:#4a9,color:#fff
    style E fill:#4a9,color:#fff
```

### シナリオ5: 編集内容が黙って消えない（K1, KA4）★このステアリングの核心

> **このステアリングはこのバグを修正するために存在する。ステアリング完了時にこの現象は抑制されていなければならない。**

```gherkin
Given タイルマップエディタで城タイルを配置した
And 保存してゲームに反映した
When ゲームをリロードする
Then 城タイルが草や別のタイルに勝手に変わっていない
And 保存した内容がそのまま表示される
```

```mermaid
graph TD
    A[城タイルを配置] --> B[保存]
    B --> C[ゲームに反映]
    C --> D{リロード後}
    D -->|正常| E[城タイルが<br/>そのまま表示される]
    D -->|バグ: 黙って化ける| F[草に変わっている<br/>★絶対に発生させない]

    style E fill:#4a9,color:#fff
    style F fill:#a44,color:#fff
```

### シナリオ6: 編集でゲームが起動しなくならない（K3, KA2）

```gherkin
Given 子どもがタイルマップを自由に編集した
And 想定外の組み合わせでタイルを配置した
When 保存してゲームをリロードする
Then ゲームが正常に起動する
And エラー画面が表示されない
```

```mermaid
graph TD
    A[子どもが自由に編集] --> B[想定外の<br/>タイル組み合わせ]
    B --> C[保存→リロード]
    C --> D{結果}
    D -->|正常| E[ゲームが起動する<br/>多少変でも動く]
    D -->|NG| F[エラーで画面が落ちる<br/>★絶対に発生させない]

    style E fill:#4a9,color:#fff
    style F fill:#a44,color:#fff
```

### シナリオ7: 子どもがマウスで操作できる（KR2, KA5）

```gherkin
Given 子どもがタイルマップエディタの前に座っている
When タイルパレットからタイルを選ぶ
And マップ上の好きな場所をクリックする
Then タイルが配置される
And 「触ってはいけない場所」が存在しない
```

```mermaid
graph TD
    A[子どもがマウスを握る] --> B[パレットから<br/>タイルを選ぶ]
    B --> C[マップ上を<br/>クリック]
    C --> D[タイルが配置される]
    D --> E[どこを触っても<br/>壊れない]

    style A fill:#49a,color:#fff
    style E fill:#4a9,color:#fff
```

---

## 成功基準

| 基準 | 内容 |
|---|---|
| 反映 | 編集した内容が必ずゲームに反映される（K1） |
| 速度 | 1サイクル（編集→確認）が2分以内（K2） |
| 安全 | 子どもが自由に触ってもゲームが壊れない（K3, KA2） |
| 自然さ | オートタイルが自動で繋がる（K4） |
| 操作性 | 子どもがマウスだけで完結できる（KR2） |
| 復元 | Ctrl+Zで元に戻せる（T5） |

### 成功基準の検証フロー

```mermaid
graph TD
    V1[タイルを配置] --> V2{反映された?}
    V2 -->|はい| V3[オートタイル確認]
    V2 -->|いいえ| F1[反映基準 NG]

    V3 --> V4{自然に繋がった?}
    V4 -->|はい| V5[Ctrl+Z確認]
    V4 -->|いいえ| F2[自然さ基準 NG]

    V5 --> V6{元に戻った?}
    V6 -->|はい| V7[変なタイル配置]
    V6 -->|いいえ| F3[復元基準 NG]

    V7 --> V8{ゲーム起動する?}
    V8 -->|はい| PASS[全基準 OK]
    V8 -->|いいえ| F4[安全基準 NG]

    style PASS fill:#4a9,color:#fff
    style F1 fill:#a44,color:#fff
    style F2 fill:#a44,color:#fff
    style F3 fill:#a44,color:#fff
    style F4 fill:#a44,color:#fff
```

---

## 参照

- [`./journey.md`](./journey.md) — このジャーニーの体験設計
- [`./problem.md`](./problem.md) — 課題定義
